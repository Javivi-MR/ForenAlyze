# Example environment configuration for Forenalyze
# Copy this file to `.env` (which is gitignored) and fill in
# the values appropriate for your local or deployment environment.

# Flask secret key (MANDATORY in production)
SECRET_KEY=change-me-in-production

# Database URL (PostgreSQL)
# Example for local Docker postgres:
# DATABASE_URL=postgresql+psycopg2://forenalyze:forenalyze@localhost:5432/forenalyze
DATABASE_URL=

# Per-user storage quota in MB (default: 2048)
STORAGE_QUOTA_MB=2048

# Optional path to clamscan/clamscan.exe
# If empty, "clamscan" will be used from the PATH
CLAMAV_PATH=

# VirusTotal API configuration
# Public or academic API key (hash lookups only)
VIRUSTOTAL_API_KEY=
# Enable/disable all VirusTotal requests (true/false)
VIRUSTOTAL_ENABLED=true
# In-memory cache TTL for VirusTotal responses (seconds)
VIRUSTOTAL_CACHE_TTL_SECONDS=3600

# YARA configuration
# Enable YARA scanning (true/false)
YARA_ENABLED=false
# Path to YARA rules file or directory containing multiple .yar files
# Examples:
#   YARA_RULES_PATH=/opt/forenalyze/rules/forenalyze.yar
#   YARA_RULES_PATH=/opt/forenalyze/rules
YARA_RULES_PATH=

# Sandbox / dynamic analysis hook configuration
# Enable the sandbox integration hook (true/false)
SANDBOX_ENABLED=false
# Sandbox mode: "disabled" (default), "mock" (synthetic demo results),
# "file" (load PoC results from a JSON file produced by Cuckoo/other
# sandbox) or "hybrid_analysis" (submit file to Hybrid Analysis via API).
SANDBOX_MODE=disabled
# Optional path to a JSON file with sandbox results
# Only used when SANDBOX_MODE=file, e.g.:
#   SANDBOX_MOCK_RESULT_PATH=/opt/forenalyze/sandbox_result_example.json
SANDBOX_MOCK_RESULT_PATH=

# Hybrid Analysis (Falcon Sandbox) remote integration (optional)
# These settings are used when SANDBOX_MODE=hybrid_analysis and you
# have a community/academic account with API access.
#
# API key for Hybrid Analysis
HYBRID_ANALYSIS_API_KEY=
# Base API URL (v2). La documentación oficial usa el host sin "www".
HYBRID_ANALYSIS_API_URL=https://hybrid-analysis.com/api/v2
# Public web URL for viewing samples
HYBRID_ANALYSIS_PUBLIC_URL=https://www.hybrid-analysis.com
# Environment identifier used by Hybrid Analysis (depends on your
# account and documentation; 100 is a common Windows environment ID
# in many examples, but you should verify it in your account).
HYBRID_ANALYSIS_ENV_ID=
